node {

    // def toolbelt = tool 'toolbelt'
  
    stage("build") {
        git
    }

    stage("test") {
        echo 'inside test stage'
    }

    stage("deploy") {
        echo 'inside deploy stage'
    }
}